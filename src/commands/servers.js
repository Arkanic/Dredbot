module.exports = {
  name: "servers",
  description: "View all the servers that the bot is on",
  documentation: "View all the servers that the bot is on",
  execute(message, cache, client) {
    let msg = "";
  }
}